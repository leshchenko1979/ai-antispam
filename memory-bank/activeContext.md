## Active Context

- **Silent auto-delete mode (2026-05)**: ✅ **Shipped** — code + phase 1 migration + deploy; channel post https://t.me/ai_antispam/43; admin broadcast 252 DMs (2026-05-16). **Remaining**: phase 3 `--drop-delete-spam` when stable.
- **Logfire trace 019e5fe5... cleanup fix (2026-05-27)**: ✅ **Complete** — `perform_complete_group_cleanup` now treats `TelegramBadRequest: chat not found` as "already absent in Telegram" and still executes DB cleanup. This prevents repeated no-rights scheduled-job exceptions for stale group IDs and preserves admin notification flow for successfully cleaned records. Test: `tests/common/test_notifications.py::test_perform_complete_group_cleanup_chat_not_found_still_cleans_db`.
- **Test Infrastructure Fix (2026-05-16)**: ✅ **Fixed** — SQLite adapter in `tests/conftest.py` now preserves `RETURNING` clause and supports `ON CONFLICT DO UPDATE` queries. Fixed unique index mismatch for pending spam examples in schema and migrations.
- **Copy Polish (2026-05-16)**: ✅ **Complete** — Replaced "DM" with "private message" / "личное сообщение" in locales and clarified the purpose of the "Not spam" button (fixing false positives) based on user feedback. Updated `opsPlaybook.md` with these copy rules.
- **Member probation (2026-05)**: ✅ Shipped — `moderation_event_count`, edited-message moderation for probation members, `message_trusted_member_skipped`. **Deploy**: run `--add-moderation-event-count` on DB before new image. **Broadcast**: Sent update notification to 268 admins via VDS script.
- **MTProto thread context (2026-04)**: ✅ **Complete** — `establish_context_via_thread_reading` uses `messages.getReplies` on the **main channel** peer when `main_channel_id` / `main_channel_username` are set (`msg_id`: `original_channel_post_id` → thread/reply); otherwise discussion group. Logging: `context_source` `main_channel` vs `discussion_group`. Tests: `tests/spam/test_thread_get_replies_peer.py`. MTProto optional-context failures (`thread-based context establishment`, `group message reading`) log at **WARNING** so `TelegramLogHandler` forwards them (not DEBUG-only).
- **pydantic-ai Migration (2026-04-21)**: ✅ **Complete** — Migrated from raw aiohttp HTTP calls to pydantic-ai agents. New `src/app/agents.py` with Gateway/OpenRouter agents, structured output via `SpamClassification`. Fixed double `/v1/v1` URL bug in both providers. Logfire tracing fixed: `send_to_logfire="if-token-present"` overrides pytest default, `instrument_pydantic_ai()` + `instrument_openai()` enable full span chain. `environment="production"` for prod, `"testing"` for integration tests. All integration tests pass with full spans visible in Logfire cloud.
- **Spam/Ham Logic (is_spam + confidence)**: ✅ **Complete** — Removed `spam_score > 50` rule. Classifier now returns `(is_spam, confidence, reason)`; pipeline branches on `is_spam` directly. Spam with confidence <90% → notify only; not-spam with confidence <90% → review.
- **Low-Confidence Review Flow**: ✅ **Complete** — Messages with confidence <90% (both spam and not-spam) now go to admin review. Not-spam with low confidence: optimistic add to approved_members, notify with "Low confidence — please review" title and confidence hint; admins confirm via "Удалить"/"Не спам". Returns `message_low_confidence_review`. Locale keys: `spam.review_low_confidence_title`, `spam.review_low_confidence_hint`.
- **Spam Prompt Consistency**: ✅ **Complete** - Fixed system prompt inconsistencies: removed unused -100/+100 score scale, fixed typo (intensity→intensive), removed XML wrapper from examples, normalized blank line formatting, unified empty section phrasing (ACCOUNT AGE uses photo_age=unknown). **2026-04-05**: Both few-shot examples and live requests use unified JSON format: `{"examples": [{"input": {...}, "label": {"is_spam": bool, "confidence": int}}]}` for examples; live request returns `{"message": "...", "user_name": null, "user_bio": null, "linked_channel": null, "stories": null, "reply_context": null, "account_signals": null}`. Removed yaml import from prompt_builder.py.
- **Prompt Calibration Cleanup (2026-03-26)**: ✅ **Complete** - Prompt text only: softened absolute Trojan-Horse wording, added explicit high-confidence evidence gate (`90+` requires concrete message cues or multiple strong profile cues), and added confidence-calibration policy (use medium confidence for ambiguous mixed/weak signals). Kept few-shot example schema and DB-derived confidence unchanged. Validation: targeted tests in `tests/common/test_spam_classifier.py` pass; review artifact re-rendered in `docs/logfire_trace_019d2446_prompt_messages.json`.
- **Account signals + Premium (2026-03)**: ✅ **Complete** — `SpamClassificationContext` uses `profile_photo_age` (MTProto) and `is_premium` (Bot API); LLM user block **`ACCOUNT SIGNALS`** bundles photo line + `is_premium`; DB column `account_signals_context` (migration `--rename-account-signals-context`). System prompt: `add_account_signals_guidance`, Trojan Horse / reply-thread carve-outs to cut false positives on polite questions under promo replies. Shared `build_account_signals_body` for cache and pending examples.
- **Spam Prompt vc.ru Article Alignment**: ✅ **Complete** - Aligned system prompt with vc_ru_article.md and confirmedSpamExamples.md: added Trojan Horse section and signal hierarchy, expanded USER INFO (income in name, bot links, "чекай канал в био"), REPLY CONTEXT caveat (relevant alone does not mean legitimate), KNOWLEDGE SHARING concrete phrases, LINKED CHANNEL gasket pattern, typo fix (incomprehensible), reply-coherence phrasing.
- **Spam Examples Cleanup**: ✅ **Complete** - Created `scripts/cleanup_admin_spam_examples.py` for admin-specific cleanup: --remove-contradicting (not-spam with t.me/bot in bio), --remove-duplicates, --limit N. Ran cleanup for admin 286024235 (Chat Павел Боровиков): removed 4 contradicting, limited to 30 admin-specific.
- **Spam Examples Config**: ✅ **Complete** - Limit and ham proportion in config.yaml: `spam.examples_limit` (40), `spam.examples_ham_ratio` (0.25). spam_ratio = 1 - ham_ratio. get_spam_examples fetches 25% ham and 75% spam, most recent within each category, combined by created_at desc.
- **Key Decisions**:
  - **Cloudflare Primary Provider**: All bot operations (spam classification, private chat) now use Cloudflare AI Gateway exclusively.
  - **Spam Tactics File**: Created a dedicated `memory-bank/spamTactics.md` to track evolving spam patterns and examples.
  - **Spam Tactics Rule**: Implemented `.cursor/rules/spam-tactics.mdc` to ensure the memory bank is updated when new spam is discovered.
- **Recent Implementation**:
  - **VC.ru Article & Tactics Expansion**: ✅ **Published & Announced** - Restructured `memory-bank/vc_ru_article.md` and published on vc.ru: [vc.ru/telegram/2794831-kak-spam-v-telegram-maskiruetsya-pod-cheloveka](https://vc.ru/telegram/2794831-kak-spam-v-telegram-maskiruetsya-pod-cheloveka). Broadcasted announcement to 270 bot admins. Restored "News and Guides" section on landing page with the article link.
  - **Spam Tactics Memory Bank**: ✅ **Complete** - Initialized `spamTactics.md` with data from `@ai_antispam` and database, including the detailed Knowledge Sharing to Extortion funnel and Coordinated Group tactics.
  - **Spam Tactics Cursor Rule**: ✅ **Complete** - Added `.cursor/rules/spam-tactics.mdc` for persistent maintenance.
  - **Spam Classifier Migration**: ✅ **Complete** - Updated spam classification to use Cloudflare instead of OpenRouter.
  - **Private Chat Migration**: ✅ **Complete** - Updated private message handling to use Cloudflare for admin interactions.
  - **Environment Configuration**: ✅ **Complete** - Fixed environment variable confusion (restored OpenRouter URL, separated Cloudflare config).
  - **Code Cleanup**: ✅ **Complete** - Removed `bs4` (BeautifulSoup) dependency and refactored `sanitize_llm_html` to use regex, reducing overhead and removing unused dependencies. Updated HTML tag support to exactly match official Telegram Bot API specifications including all documented tags.
  - **LLM Provider Resilience**: ✅ **Complete** - Implemented optimized fallback strategy: Cloudflare (1 fast try) → OpenRouter (3 tries with model rotation). Cloudflare fails fast to avoid delays, OpenRouter handles retries and model fallbacks for maximum reliability.
- **pydantic-ai Migration (2026-04-21)**: ✅ **Complete** — Replaced raw aiohttp/httpx LLM calls with pydantic-ai agents. `src/app/agents.py` defines Gateway + OpenRouter agent pools with structured output. Fixed double `/v1/v1` URL bug in base URL construction. Logfire integration: `instrument_pydantic_ai()` + `instrument_openai()` + `send_to_logfire="if-token-present"`. Environment isolation: `environment="production"` (main.py) vs `"testing"` (pytest). Full span chain visible in Logfire cloud: pytest → test function → spam_classifier_gateway_call → agent run → chat → Chat Completion.
- **Recent Implementation**:
  - **LLM Model Evaluation**: ✅ **Complete** - Comprehensive evaluation infrastructure with balanced test cases and JSON result storage.
  - **Memory Bank Documentation**: ✅ **Complete** - Updated systemPatterns.md to document Cloudflare AI Gateway usage.
  - **Logfire Trace Analysis**: ✅ **Complete** - Identified and fixed `TelegramBadRequest: message to delete not found` in `handle_spam_confirm_callback`. Flow now continues with ban/confirmation even if message was already deleted.
  - **Logfire Instrumentation Cleanup**: ✅ **Complete** - Implemented approved Logfire pattern: auto-tracing for modules with `@logfire.no_auto_trace` decorators on manually instrumented functions to prevent span duplication.
  - **Logfire Context Lookup Fix**: ✅ **Complete** - Fixed `find_spam_classification_context` two-step trace lookup. Reply, stories, and account context are now correctly extracted from Logfire when adding spam examples from forwarded messages. Unit tests in `tests/common/test_logfire_lookup.py`. (Live path: PostgreSQL `message_lookup_cache` + `account_signals_context`.)
- **Callback Button UX Pattern**: ✅ **Complete** - Consistent pattern for action-completion callbacks: remove inline keyboard and append confirmation line to message. Applied to `handle_spam_confirm_callback` (edit_message_text with confirmation), `process_spam_example_callback` (reply_markup=None). Exception: `buy_stars` leaves message as-is (invoice provides immediate feedback). Documented in systemPatterns.md.
- **Telegram error handling (2026-05)**: ✅ **Complete** — `@dp.errors()` handler, `telegram_errors.py` helpers (`is_permission_error`, `is_message_not_found_error`, `is_webhook_retryable`), conservative webhook 503, extended `retry_on_network_error` for aiogram transient API errors. Domain flows unchanged: `handle_permission_error`, inaccessible cleanup, no-rights grace.
- **Immediate Next Steps**:
  - Prod: `--drop-delete-spam` after stable smoke on silent mode.
- **Agent ops reference**: `memory-bank/opsPlaybook.md` — broadcast `--new-campaign`, silent-mode copy rules, `PG_HOST` for local migrations.
- **HTML Hyperlink Preservation**: ✅ **Fixed** - Modified `sanitize_llm_html()` to preserve `<a>` tags in private chat responses, ensuring hyperlinks from LLM-generated content are now clickable instead of being stripped.
- **MTProto Spam Notifications**: ✅ **Feature-flagged** - MTProto-based promotional notifications to human spammers / spamming channel admins are now controlled by `spam.notify_spammers_via_mcp` and env override `SPAM_NOTIFY_SPAMMERS_VIA_MCP`. Default is **off** to avoid unsolicited outbound messages and Telegram penalties. Existing MCP fixes remain in place (chat_id as string, lowercase parse_mode, SSE handling in `McpHttpClient`).
- **Linked Channel & Display Utils**: ✅ **Complete** - Linked channel collected from profile (linked) > bio > message (first mention only). `LinkedChannelSummary` has `channel_source` ("linked"|"bio"|"message") and `channel_id`. DRY display util `format_chat_or_channel_display(title, username)` used everywhere for "Title (@username)". New-user /start uses Bot API `get_chat()` for safe linked channel (profile + bio) collection before MTProto fallback, appending offer to protect that channel.
- **Obscure Webhook Path**: ✅ **Complete** - Webhook moved from `/` to `/process-tg-updates`. Traefik rule restricted to `Path(/process-tg-updates) || Path(/health)`. GET removed from webhook handler; only POST accepted. Scanner bots probing `/`, `/?author=1`, and WordPress paths now hit 404 at Traefik before reaching the app. Webhook URL set on startup via `bot.set_webhook()`.
- **Channel-Add Userbot Fallback**: ✅ **Complete** - When bot is added to a channel, primary flow (notify admins via Bot API, leave) runs first. On `TelegramForbiddenError` (e.g. bot not a member), falls back to userbot DM to adding user if they have a username. Message includes preamble explaining sender and context. New `userbot_messaging.send_userbot_dm` and `build_channel_instruction_userbot_message`. Unit tests in `test_channel_management.py`.
- **Spam Examples Pending Flow**: ✅ **Complete** - Implemented unified `spam_examples` with `confirmed` flag. At notify, full payload stored via `insert_pending_spam_example`; callback data passes only DB id. `confirm_pending_example_as_not_spam` returns row for unban/add_member; no fallback if not found. Migration: `--add-pending-spam-columns`.
- **Spam Example Delete Confirmation Fix**: ✅ **Complete** - When admin presses "Удалить" (Delete), `confirm_pending_example_as_spam` now updates the pending row: `confirmed=true`, `admin_id` (admin who pressed), `score=100`. Fixes admin_id=NULL (wrongly common) and unconfirmed rows. Lookup by (chat_id, message_id). **One-time remediation** (2026-03): 15 orphaned rows (IDs 971-985) where admin had clicked Delete pre-fix were backfilled with admin_id, confirmed=true, score=100 using Logfire "Banned user" trace correlation.
- **Group Command Unification**: ✅ **Complete** - `_delete_and_redirect_to_pm()` in command_handlers: delete + answer for all commands in groups. handle_help_command returns early for groups; handle_group_commands calls it for /stats, /mode, /ref and others.
- **Admin Low-Balance Warning System**: ✅ **Complete** - Proactive warnings: week-ahead when credits low, day-1/day-6/day-7 depletion timeline. Asyncio daily loop in `src/app/background_jobs/`. On deadline bot leaves sole-payer groups (no account deletion). Migration `--add-low-balance-columns`.
- **No-Rights Grace Period**: ✅ **Complete** - When bot detects no required rights (delete_messages, restrict_members), sets `no_rights_detected_at`, notifies admins, and leaves after grace period via daily job. Detection: permission error, bot added without rights, rights revoked. Clear when rights restored. Config: `billing.no_rights_grace_days` (default 7). Migration `--add-no-rights-column`.
- **Bot and Landing i18n (RU/EN)**: ✅ **Complete** - Added `language_code` column to administrators (migration `--add-language-code`), locale files `src/app/locales/ru.yaml` and `en.yaml`, `/lang` command with RU/EN buttons. All handlers use `t(lang, key)` and `resolve_lang()`. LLM spam prompt explanation varies by admin language. Landing: `index.html` (EN) and `index-ru.html` (RU) with language switcher in header/footer. Build processes both HTML files.
- **i18n key audit & help callbacks**: ✅ **Complete** - Help inline `callback_data` values match locale keys (`help.getting_started`, etc.); `HELP_PAGE_CALLBACK_KEYS` in `i18n.py`; `F.data.in_(HELP_PAGE_CALLBACK_KEYS)`. Audit logic in `tests/support/i18n_keys_audit.py` (dev/test only); `python3 scripts/audit_i18n.py`; pytest `tests/test_i18n_keys_audit.py`; `pytest.ini` `pythonpath = src .` for `tests.support` imports.
- **Landing Consultant Recommendations**: ✅ **Complete** - Hero restructured with USP (AI Telegram bot that catches spam others miss), subhero with 1-min setup / 24/7 / 100 free checks. Mermaid infographics (profile analysis vs text-only, spam detection flow). Dedicated Pricing section (100 free, 1 Star per check, message analysis free). New FAQ items (bother users, train for community). Softened false-positives claim. Social proof and screenshots omitted per constraints.
- **Telegram Deep Links**: ✅ **Complete** - Landing and bot use `?startgroup&admin=delete_messages+restrict_members` for one-click add with pre-granted permissions. `get_add_to_group_url()` in utils reads `bot_username` from config. Help messages (getting_started, group_redirect, permission_steps) promote the link. Deactivate and low-balance CTAs use add-to-group URL. /start with param (landing, setup_*) in groups shows brief ready message.